import React, { Fragment } from 'react';

export default () => { return (
    <Fragment>
        <div id={ 'header' }>Contact Me</div>
        <div id={ 'export' }>
            Foo
        </div>
    </Fragment>
)}